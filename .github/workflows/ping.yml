name: keep-alive

on:
  schedule:
    - cron: "*/20 * * * *"  
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health
        run: |
          curl -fsS "https://sour-evey-no-drama-club-eab61b7a.koyeb.app/health" > /dev/null
